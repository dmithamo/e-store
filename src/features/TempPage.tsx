import React from 'react';

export default function WorkingOnIT() {
  return (
    <div
      style={{
        display: 'flex',
        alignItems: 'center',
        justifyContent: 'center',
        width: '100%',
        height: '90vh',
      }}
    >
      Working on IT &trade;
    </div>
  );
}
